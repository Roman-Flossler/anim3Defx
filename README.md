# This effect is now part of the Grid overflow

CSS layout based on CSS grid with optional animated 3D effect, utility classes and adjustable via CSS variables.

 »»» https://github.com/Roman-Flossler/Grid-Overflow
